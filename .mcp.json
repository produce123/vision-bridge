{
  "mcpServers": {
    "vision-bridge": {
      "type": "stdio",
      "command": "node",
      "args": ["src/index.js"],
      "env": {
        "ARK_API_KEY": "${ARK_API_KEY}",
        "ARK_MODEL_ID": "${ARK_MODEL_ID:-doubao-seed-2-0-lite-260428}"
      }
    }
  }
}
